name: Pa1Whisper
options:
  bundleIdPrefix: com.pa1whisper
  deploymentTarget:
    macOS: "14.0"
  minimumXcodeGenVersion: "2.38"
  groupSortPosition: bottom

settings:
  base:
    SWIFT_VERSION: "5.10"
    MACOSX_DEPLOYMENT_TARGET: "14.0"

packages:
  WhisperKit:
    url: https://github.com/argmaxinc/WhisperKit.git
    from: "0.9.0"

targets:
  Pa1Whisper:
    type: application
    platform: macOS
    sources:
      - path: Pa1Whisper
        excludes:
          - Info.plist
          - "*.entitlements"
    settings:
      base:
        INFOPLIST_FILE: Pa1Whisper/Info.plist
        CODE_SIGN_ENTITLEMENTS: Pa1Whisper/Pa1Whisper.entitlements
        PRODUCT_BUNDLE_IDENTIFIER: com.pa1whisper.app
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: 1
        ENABLE_HARDENED_RUNTIME: true
    dependencies:
      - package: WhisperKit
        product: WhisperKit
    entitlements:
      path: Pa1Whisper/Pa1Whisper.entitlements
